"""Extraction sub-package — bronze → silver → knowledge graph pipeline.

Sub-modules:
- parsing: document parsing (PDF, HTML, Excel, DOCX)
- pipeline: (future) orchestrates parse → extract → KG write
- export: (future) markdown → DOCX/XLSX deliverable generation
"""
