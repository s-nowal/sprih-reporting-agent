"""
End-to-end tests package.

Tests exercise full HTTP request/response cycles through the FastAPI
TestClient, simulating the SDK call sequence the frontend uses.
Agent service is mocked at the AgentService boundary.
"""
